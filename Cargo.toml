[package]
name = "binary_utils"
version = "0.3.0"
authors = ["Bavfalcon9"]
edition = "2021"
include = ["src/**/*", "README.md"]

[dependencies]
codegen = { path = "./codegen", version = "0.1.0", optional = true }
bytes = "1.4.0"

[features]
default = [ "codegen" ]